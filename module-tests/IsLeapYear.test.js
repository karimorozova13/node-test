const isLeapYear = require("./isLeapYear");

// first argument - name of test, what we are testing
describe("test of isLeapYear function", () => {
  test("test if year divides by 4 without rest - true", () => {});
  test("test if year divides by 100,4 without rest -false", () => {});
  test("test if year divides by 400,100,4 without rest - true", () => {});
});
